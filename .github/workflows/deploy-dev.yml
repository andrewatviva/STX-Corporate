name: Deploy to Dev (stx-corporate-dev)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Functions dependencies
        run: cd functions && npm ci

      - name: Build
        run: npm run build
        env:
          REACT_APP_FIREBASE_API_KEY:            ${{ secrets.DEV_FIREBASE_API_KEY }}
          REACT_APP_FIREBASE_AUTH_DOMAIN:        ${{ secrets.DEV_FIREBASE_AUTH_DOMAIN }}
          REACT_APP_FIREBASE_PROJECT_ID:         ${{ secrets.DEV_FIREBASE_PROJECT_ID }}
          REACT_APP_FIREBASE_STORAGE_BUCKET:     ${{ secrets.DEV_FIREBASE_STORAGE_BUCKET }}
          REACT_APP_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.DEV_FIREBASE_MESSAGING_SENDER_ID }}
          REACT_APP_FIREBASE_APP_ID:             ${{ secrets.DEV_FIREBASE_APP_ID }}
          REACT_APP_FIREBASE_MEASUREMENT_ID:     ${{ secrets.DEV_FIREBASE_MEASUREMENT_ID }}
          REACT_APP_NUITEE_API_KEY_SANDBOX:      ${{ secrets.NUITEE_API_DEV }}
          REACT_APP_NUITEE_API_KEY_PROD:         ${{ secrets.NUITEE_API_PROD }}

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEV }}

      - name: Deploy to Firebase Hosting (Dev)
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEV }}
          projectId: stx-corporate-dev
          channelId: live

      - name: Deploy Cloud Functions, Firestore and Storage rules (Dev)
        run: npx firebase-tools deploy --only functions,firestore:rules,storage --project stx-corporate-dev

